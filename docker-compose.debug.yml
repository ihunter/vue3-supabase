version: '3.9'

services:
  vuetwitterclone:
    image: vuetwitterclone
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: development
    ports:
      - 3000:3000
      - 9229:9229
    command: ["npm", "start"]
